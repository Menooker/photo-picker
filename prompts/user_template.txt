## 任务
分析以下日期拍摄的所有照片，返回 JSON 分类结果。

## 日期
{{DATE}}

## 照片数据
以下是每张照片的元数据和对应图片（格式：========== 分隔 - 元数据 JSON - 图片）
元数据包括照片id，时间（ISO格式），地点（格式为维度,经度；南方取负，西经取负）。元数据之后就是照片

## 输出格式
返回一个 JSON 对象，results 数组包含每张照片的分类结果，返回每个照片对应id的评判结果：
```json
{"results": [{"id": "xxx", "action": "DELETE|KEEP_PHONE|KEEP_PC|UNDECIDED", "confidence": 0.9, "reason": "原因"}]}
```
